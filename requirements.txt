# Core: CockroachDB-backed LangChain memory (vector store, chat history, checkpointer)
langchain-cockroachdb>=0.3.0
langchain-core>=0.3.0
langgraph>=0.2.0

# CockroachDB driver used by langchain-cockroachdb / SQLAlchemy async
psycopg[binary]>=3.2

# AWS Lambda + S3
boto3>=1.34

# PDF uploads on the Review page (text layer only — no OCR)
pypdf>=5.1

# Config
python-dotenv>=1.0

# Real LLM path (MOCK_MODE=false). Gemini has a free tier that needs no card.
langchain-google-genai>=4.3.2

# OPTIONAL — only if you prefer OpenAI over Gemini.
# langchain-openai>=0.2
