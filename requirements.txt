# Core: CockroachDB-backed LangChain memory (vector store, chat history, checkpointer)
langchain-cockroachdb>=0.3.0
langchain-core>=0.3.0
langgraph>=0.2.0

# CockroachDB driver used by langchain-cockroachdb / SQLAlchemy async
psycopg[binary]>=3.2

# AWS Lambda + S3
boto3>=1.34

# Config
python-dotenv>=1.0

# OPTIONAL — only needed when MOCK_MODE=false. Uncomment to use real LLM/embeddings.
# langchain-openai>=0.2
